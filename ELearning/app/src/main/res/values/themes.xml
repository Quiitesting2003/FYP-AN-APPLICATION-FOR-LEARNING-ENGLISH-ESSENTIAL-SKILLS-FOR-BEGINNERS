<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.ELearning" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Main theme, inheriting from Base.Theme.ELearning -->
    <style name="Theme.ELearning" parent="Base.Theme.ELearning">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimarys</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDarks</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryDark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/colorSecondary</item>

        <!-- Customize your theme here. -->
    </style>

    <!-- No Action Bar theme, inheriting from Theme.ELearning -->
    <style name="NoActionBarTheme" parent="Theme.ELearning">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimaryDark">@color/colorPrimarys</item>
        <item name="colorAccent">@color/black</item>
    </style>

</resources>
